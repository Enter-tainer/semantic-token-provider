{
  "name": "@mgtd/semantic-token-provider",
  "version": "1.0.1",
  "description": "providing semantic tokens",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc -watch -p ./",
    "build": "tsc",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mgt",
  "license": "MIT",
  "dependencies": {
    "vscode-jsonrpc": "^5.0.1",
    "vscode-languageserver-protocol": "^3.16.0-next.6",
    "vscode-uri": "^2.1.2"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/node": "^14.0.27",
    "typescript": "^3.9.7"
  },
  "publishConfig": {
    "access": "public"
  }
}
